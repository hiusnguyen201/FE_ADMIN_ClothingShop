export type Province = {
  code: string;
  name: string;
  unit: string;
};

export type District = {
  code: string;
  name: string;
  unit: string;
  full_name: string;
};

export type Ward = {
  code: string;
  name: string;
  unit: string;
  full_name: string;
};
