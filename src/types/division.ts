export type Province = {
  ProvinceID: number;
  ProvinceName: string;
};

export type District = {
  DistrictID: number;
  DistrictName: string;
};

export type Ward = {
  WardCode: string;
  WardName: string;
};
